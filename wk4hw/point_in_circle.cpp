#include <iostream>

using namespace std;

int main() {
    int pointx, pointy, circlex, circley, radius;

    cout << "Please input the coordinates of the point as (x, y)" << endl;
    cin >> pointx >> pointy;

    cout << "Please input the coordinates of the circle and its radius as (x, y, radius)" << endl;
    cin >> circlex >> circley >> radius;

    
}